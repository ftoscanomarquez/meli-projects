# RETROSPECTIVA.md — Planet Scape of the Solar System

> Bitácora obligatoria y permanente de problemas encontrados durante la implementación y cómo se resolvieron.
> Documento mandatorio por la skill `toscaprompt` — se mantiene siempre, incluso vacío al inicio.
> Sincronizado con [`AGENTS.md`](./AGENTS.md) — ver regla de sincronización en [AGENTS.md §0.1](./AGENTS.md#01-regla-de-sincronizacion-de-documentacion): todo error detectado durante implementación se registra aquí, no solo en el Changelog de especificación.

---

## Formato

| Fecha | Fase | Problema | Causa raíz | Solución |
|---|---|---|---|---|

---

## Registro

| Fecha | Fase | Problema | Causa raíz | Solución |
|---|---|---|---|---|
| 2026-07-26 | Pre-producción — Fase 6 (GitHub Actions/CI, `docs/PRE-PROD.md`) | Tras validar localmente cada comando del workflow (`npm run lint/typecheck/test/build`) y darlo por listo, el primer `git push` no disparó ningún run de GitHub Actions — `gh run list` no mostraba nada. | El archivo `ci.yml` se había creado en `planet-scape/.github/workflows/ci.yml`, pero GitHub Actions solo descubre workflows en `.github/workflows/` exactamente en la RAÍZ del repositorio — y la raíz git real de este repo es `D:/meli-projects` (un nivel arriba de `planet-scape/`, donde vive todo el código del proyecto, confirmado desde el commit inicial de la sesión anterior). Validar los comandos sueltos localmente no detecta este tipo de error, porque nunca se ejecuta el mecanismo real de descubrimiento de workflows de GitHub. | Se movió el archivo a `.github/workflows/ci.yml` (raíz real del repo, un nivel arriba de donde vivía) y se agregó `defaults.run.working-directory: planet-scape` a cada job (para que los comandos sigan ejecutándose dentro de la carpeta del proyecto) más un filtro `paths` para que el workflow solo reaccione a cambios reales de este proyecto. Confirmado con `gh run list` que el siguiente push sí disparó el run. |
| 2026-07-26 | Pre-producción — Fase 6 (GitHub Actions/CI) | Con el workflow ya disparándose correctamente, los 4 jobs fallaron en el paso `npm ci` con `npm error code EUSAGE` (`package.json and package-lock.json ... are in sync` / `Missing: @swc/helpers@0.5.23 from lock file`) — en un runner Linux limpio de GitHub, nunca visto en la máquina de desarrollo (Windows). | El `package-lock.json` tenía una inconsistencia menor con `package.json` (probablemente arrastrada de las varias instalaciones con `--legacy-peer-deps`/`overrides` de la Fase 5, que ajustan la resolución sin siempre dejar el árbol de dependencias perfectamente consistente). `npm install` en Windows la toleraba en silencio; `npm ci` (usado en CI a propósito, más estricto que `npm install`) la rechaza por diseño — su función es instalar EXACTAMENTE el árbol congelado en el lockfile, sin resolver nada nuevo. | `rm -rf node_modules package-lock.json && npm install` completo desde cero para regenerar un lockfile realmente consistente, validado localmente con `npm ci --dry-run` (sin error) antes de volver a subirlo. Los 4 jobs pasaron en verde en el siguiente push, confirmado real en `github.com/ftoscanomarquez/meli-projects/actions`. **Lección**: para CI/CD, simular los comandos localmente no sustituye ver el workflow correr de verdad en la plataforma de destino al menos una vez — hay diferencias de entorno (ubicación exacta del archivo, SO del runner, estrictez de `npm ci` vs `npm install`) que solo se manifiestan ahí, no en ninguna simulación local. |
| 2026-07-26 | Pre-producción — Fase 6 (GitHub Actions/CI, `docs/PRE-PROD.md`) | Antes de escribir el workflow de CI, se corrió `npm run test` como chequeo previo — 4 de 44 tests unitarios fallaban (`tests/unit/gameStore.test.ts`, `tests/unit/schemas.test.ts`), nunca detectado antes porque la suite nunca corría en CI. | Los tests quedaron desactualizados respecto a cambios reales de comportamiento hechos en sesiones anteriores, sin que nadie los volviera a correr: (1) `gameStore.test.ts` esperaba que perder la última vida pasara directo a `gameStatus: "gameover"` — pero desde que se implementó la secuencia de muerte (2026-07-22, ver AGENTS.md §5.3), el flujo real pasa primero por un estado intermedio `"dying"` (reacciones faciales, cuarteaduras, explosión) y solo `finishDeathSequence()` marca `"gameover"` de verdad. (2) `schemas.test.ts` tenía un test que afirmaba "solo los 4 planetas iniciales son seleccionables" y esperaba que `planet: "jupiter"` fuera rechazado — desde que Júpiter/Saturno/Neptuno se volvieron planetas premium comprables (2026-07-22/23, ver AGENTS.md §5.2/§5.4), son valores válidos del schema. (3) El mismo archivo tenía un `PlayerSearchResultSchema` de prueba con solo 5 campos (`id`/`email`/`displayName`/`stars`/`role`) — el schema real exige también `firstName`/`lastName`/`birthDate`/`phone`/`unlockedPlanets` desde que se amplió la edición de perfil del admin (2026-07-22/24, ver AGENTS.md §6.8/§9). | Los 4 tests se reescribieron para reflejar el comportamiento y esquema REALES vigentes (verificados leyendo `engine/store/gameStore.ts`/`lib/schemas/admin.ts` directamente, no adivinados) — nunca se tocó el código de producción para "hacer pasar" el test. Se agregó además un test nuevo cubriendo `finishDeathSequence()` explícitamente, que antes no tenía cobertura. **Lección**: una suite de tests que nunca corre en CI se desincroniza en silencio del código real con el tiempo — justo el tipo de problema que la Fase 6 (CI) está pensada para prevenir a futuro. |
| 2026-07-25 | Pre-producción — despliegue de PartyKit (`docs/PRE-PROD.md` Fase 5) | `npx partykit deploy` falló con `Error: You have exceeded the limit of 10000 Workers custom domains on zone 'partykit.dev'` al intentar el primer despliegue a producción. | La plataforma gestionada de PartyKit (`partykit.dev`, subdominios automáticos tipo `*.<usuario>.partykit.dev`) alcanzó un límite de infraestructura compartida de Cloudflare — no es un problema del proyecto, le puede pasar a cualquier usuario nuevo de la plataforma en ese momento. | Se intentó `partykit deploy --domain <dominio-propio>` como alternativa (evita depender de la zona compartida) — ver siguiente fila para el segundo bloqueo encontrado en ese camino. |
| 2026-07-25 | Pre-producción — despliegue de PartyKit (`docs/PRE-PROD.md` Fase 5) | `partykit deploy --domain game.planet-scape.minegocito.app` (con credenciales de Cloudflare propias: Account ID + API Token con permisos de DNS/Workers) falló con `Error: In order to use Durable Objects with a free plan, you must create a namespace using a \`new_sqlite_classes\` migration`. | Cloudflare cambió su requisito para Durable Objects en el plan gratuito (exige el nuevo tipo de migración `new_sqlite_classes`), pero el CLI `partykit` (paquete npm `partykit`, última versión publicada **v0.0.115**, sin actualizaciones desde antes de este cambio) nunca fue actualizado para generarlo — Cloudflare compró PartyKit en abril de 2024 y movió el desarrollo activo a otro paquete (ver siguiente fila). No existe ninguna versión más nueva de `partykit` en npm para actualizar, ni un flag oculto del CLI que fuerce el tipo de migración. | Investigado con un agente de exploración: Cloudflare mantiene activamente **`partyserver`** (mismo equipo, última versión en ese momento v0.5.8), una librería que corre directo sobre Wrangler + Durable Objects nativos, con **la misma forma de API** que `partykit/server` (`onConnect`/`onMessage`/`onClose`/`broadcast`/`getConnections`) y soporte real para `new_sqlite_classes`. Se migró el proyecto completo: `partykit.json` → `wrangler.jsonc` (bindings `MAIN`→`GameRoom`, `DIRECTORY`→`Directory`, con el bloque `migrations`); `party/gameRoom.ts`/`party/directory.ts` reescritos para `extends Server<Env>` en vez de `implements Party.Server` (cambios mecánicos: `this.room.id`→`this.name`, `this.room.broadcast`→`this.broadcast`, `onMessage(message, sender)`→`onMessage(sender, message)`, `room.context.parties.directory.get(id).fetch()`→RPC directo `getServerByName(env.DIRECTORY, "global")` + método público `receiveUpdate()`); `party/worker.ts` nuevo como punto de entrada (`routePartykitRequest`). El **cliente no cambió** (`lib/multiplayer/useRoomConnection.ts` sigue usando `partysocket` tal cual) — el binding se nombró `MAIN` (no `GAME_ROOM`) a propósito, porque `routePartykitRequest` deriva el segmento de URL del nombre del binding en kebab-case, y el cliente ya se conecta a `/parties/main/{roomId}` por defecto (sin pasar `party` explícito) — así no hizo falta tocar el cliente. Paquete `partykit` desinstalado del proyecto. |
| 2026-07-25 | Pre-producción — migración a `partyserver`/Wrangler (`docs/PRE-PROD.md` Fase 5) | Conflicto de peer dependencies al instalar `wrangler` junto a `partyserver`: `partyserver@0.5.8` exige `@cloudflare/workers-types@^4`, pero `wrangler@4.114.0` (la versión más reciente) exige `@cloudflare/workers-types@^5` — `npm install` fallaba con `ERESOLVE`. | Ambos paquetes son mantenidos por el mismo equipo (Cloudflare) pero se actualizaron a ritmos distintos respecto a su rango de peer dependency declarado — no hay ninguna combinación de versiones recientes de ambos que declare rangos compatibles entre sí. | Se instaló primero `wrangler@3.114.17` (última de la serie 3.x, que sí declara `@cloudflare/workers-types@^4`, compatible con `partyserver`) para poder generar `wrangler.jsonc` y validar el código sin conflictos de instalación. Sin embargo, al correr `wrangler dev` con esa versión, el Durable Object falló con `Cannot determine the name for GameRoom: this.ctx.id.name is undefined` — el propio warning de la CLI señalaba que la versión estaba desactualizada respecto al runtime que expone `ctx.id.name` correctamente. Se reinstaló `wrangler@4.114.0` con `--legacy-peer-deps` (el conflicto es solo declarativo en el `package.json`; en la práctica `partyserver` funciona bien con `workers-types@^5` en runtime) — con eso el Durable Object resolvió el nombre correctamente. **Lección**: cuando dos paquetes del mismo proveedor tienen peer dependencies cruzadas incompatibles entre sus versiones más recientes, no asumir que la versión más vieja compatible es la solución completa — puede faltarle soporte real de runtime que la versión nueva sí tiene; hay que probar el flujo funcional real (`wrangler dev` + conexión WebSocket), no solo que `npm install` no truene. |
| 2026-07-25 | Pre-producción — migración a `partyserver`/Wrangler (`docs/PRE-PROD.md` Fase 5) | Al agregar `@cloudflare/workers-types` a los `types` del `tsconfig.json` **raíz** (compartido con toda la app Next.js) para que `party/worker.ts`/`party/gameRoom.ts` reconocieran `DurableObjectNamespace`/`this.env`, `npm run typecheck` explotó con ~50 errores nuevos en componentes/rutas de la app (`Property 'stars' does not exist on type 'unknown'`, `'data' is of type 'unknown'`, etc.) sin relación aparente con PartyKit. | `@cloudflare/workers-types` sobreescribe tipos globales del DOM (notablemente `Response.json()`, que Next.js/el navegador tipan como `Promise<any>` pero Workers tipa más estricto como `Promise<unknown>`) — al declararlo en el `tsconfig.json` compartido, ese tipo más estricto se filtró a TODA la app, no solo a `party/`. | Se revirtió el cambio global y se creó un `party/tsconfig.json` **aislado** (su propio `types`, `lib`, `target`), excluyendo `party/` del `tsconfig.json` raíz (`"exclude": ["node_modules", "party"]`). Nuevo script `npm run typecheck:party` para validar ese código por separado. Confirma la regla general del proyecto de que código que corre en runtimes distintos (Node.js/Next.js vs. Cloudflare Workers) no debe compartir configuración de tipos, aunque viva en el mismo repositorio. |
| 2026-07-25 | Pre-producción — validación local de `wrangler dev` (`docs/PRE-PROD.md` Fase 5) | Tras varios intentos fallidos/interrumpidos de levantar `wrangler dev` en background (procesos matados a medias por timeouts, `disown`, `TaskStop` que no propagó la señal a hijos), el puerto 1999 quedó "LISTENING" pero **ninguna petición HTTP ni WebSocket obtenía respuesta** — ni desde `curl` (Bash/Git Bash) ni desde `Invoke-WebRequest` (PowerShell nativo, mismo stack de red que Windows), ambas con timeout total. | Diagnóstico con `Get-CimInstance Win32_Process` reveló **5 instancias completas y apiladas** de `wrangler dev` (cada una con su propio árbol `bash→node→cmd→wrangler→workerd.exe`), todas compitiendo por el mismo puerto 1999 — ninguna terminó limpio en los intentos anteriores. `workerd.exe` (el runtime que Miniflare usa para simular Cloudflare Workers localmente) es particularmente propenso a quedar huérfano en Windows cuando el proceso padre de la terminal muere sin propagar la señal de cierre al hijo — mismo patrón raíz que ya afectaba a `partykit dev` (ver `QUICK-START.md`, sección de troubleshooting ya existente antes de esta migración). | `Get-Process -Name workerd \| Stop-Process -Force` (matar TODAS las instancias, no solo la más reciente) + confirmar `Get-NetTCPConnection -LocalPort 1999` vacío antes de reintentar con una única instancia limpia — con eso la conexión WebSocket abrió y respondió correctamente en el primer intento. Documentado en `QUICK-START.md` como troubleshooting actualizado (ya no es exclusivo de `partykit dev`, aplica igual a `wrangler dev` porque comparten el mismo runtime `workerd` por debajo). **Lección para trabajo futuro con procesos en background en este entorno**: preferir siempre `run_in_background: true` del tool sobre `&`/`disown` manual en Bash, y verificar el árbol completo de procesos (no solo el puerto) antes de asumir que un "Ready on..." en el log significa que el servidor realmente está respondiendo. |
| 2026-07-24 | Login por código de 6 dígitos, redirección al celular (`lib/auth.ts`, `components/auth/AuthStatus.tsx`) | El usuario reportó que, ya con el "fix" documentado el 2026-07-22 (whitelist de orígenes de confianza en `callbacks.redirect`), el login por código desde el celular en LAN lo seguía mandando a `localhost:3000` al terminar. | El fix original solo cubría el caso de URL **absoluta** (comparando su origen contra una whitelist fija de IPs). Pero `AuthStatus.tsx#handleVerifyCode` arma `callbackUrl` como una ruta **relativa** (`/${locale}`) — el primer `if (url.startsWith("/")) return \`${baseUrl}${url}\`` del callback la interceptaba ANTES de llegar a la validación, devolviendo directo el mismo `baseUrl` interno de Auth.js que el propio comentario del fix original ya documentaba como poco confiable en este entorno (resuelve a `localhost` pese a `trustHost: true`). Además, la whitelist fija (`TRUSTED_DEV_REDIRECT_ORIGINS`, IPs hardcodeadas) exigía editar código cada vez que el celular cambiaba de red/IP — el usuario lo señaló explícitamente: "no deberian ser datos fijos estas redirecciones... urls harcodeadas". | Dos cambios: (1) `AuthStatus.tsx` ahora arma `callbackUrl` como URL **absoluta** usando `window.location.origin` (el navegador siempre conoce su propio origen real, sin necesitar adivinarlo del lado del servidor). (2) `lib/auth.ts#callbacks.redirect` ya no distingue ruta relativa/absoluta como casos separados — TODA URL se resuelve con `new URL(url, baseUrl)` y se valida su origen antes de confiar en ella, nunca se regresa `baseUrl` sin pasar por la validación. La whitelist fija se reemplazó por `isTrustedRedirectOrigin()`, que confía en cualquier origen de red local/privada (localhost, 127.0.0.1, rangos RFC 1918: 10.x/172.16-31.x/192.168.x) **solo fuera de producción** — ya no hay que editar código por cada IP de LAN nueva. **Lección para el futuro**: cuando un fix de "redirección/origen confiable" solo cubre un formato de URL (absoluta) pero el código real puede generar otro formato (relativa), el fix queda incompleto en silencio — hay que verificar TODOS los call sites que alimentan ese callback, no solo el que se probó originalmente. |
| 2026-07-24 | Modal informativo del planeta (`components/landing/PlanetInfoModal.tsx`) | El usuario reportó: "no veo todas las habilidades pasivas en la ficha informativa... definimos habilidades pasivas a marte, venus y tierra y no las veo especificadas ahi". | Cuando se agregaron las pasivas nuevas de Venus (`smallAsteroidImmune`) y Tierra (`flareImmune`+aurora) al motor (misma sesión, ronda anterior), solo se tocó `engine/abilities/*.ts`/`GameEngine.ts` — nunca se actualizó `ABILITY_SCOPE` ni `messages/*.json` (namespace `PlanetInfo`) en el modal informativo, que sigue siendo una fuente de datos separada y manual (no generada desde el código de habilidades). El modal ya soporta bien mostrar 0 o 1 pasiva por planeta (`info.passiveName` opcional) — el problema no era de renderizado, era que los datos de Venus/Tierra nunca se agregaron ahí. Marte no tiene pasiva (`marsAbility.ts` confirmado, solo activa) — el usuario lo recordaba mal, no era un tercer caso faltante. | Se agregaron `passiveName`/`passiveDescription` para Venus y Tierra en `messages/es.json`/`en.json`, y `passive: "individual"` para ambos en `ABILITY_SCOPE` (ambas pasivas se revisan solo contra `this.ability`, nunca `allyAid`, a diferencia de sus activas que sí se comparten). **Lección reforzada** (mismo patrón que la fila de Saturno/`characterSvg.ts` más abajo): agregar una habilidad nueva al motor de juego debe ir acompañado, en el mismo paso, de actualizar TODO lugar que describa las habilidades de ese planeta de cara al jugador (aquí: el modal informativo) — no solo el código que la ejecuta. |
| 2026-07-24 | Modales anidados (`components/profile/MyProfileModal.tsx`, `components/auth/AccountRecoveryModal.tsx`) | El usuario reportó que el modal de "Mi perfil" seguía apareciendo DETRÁS del carrusel de planetas, incluso después de ya haberle subido el `z-index` de su `.overlay` a 200 (mismo valor que ya había arreglado el mismo problema en `PlanetInfoModal`). | `z-index` solo se compara DENTRO del mismo contexto de apilamiento CSS. `AuthStatus.tsx` renderiza `MyProfileModal`/`AccountRecoveryModal` como hijos JSX de su propio `<div className={styles.widget}>`, y `AuthStatus.module.css` fija `.widget { position: fixed; z-index: 10; }` en escritorio — eso crea un NUEVO contexto de apilamiento. El `z-index: 200` del modal solo se compara contra otros elementos DENTRO de `.widget`; el subárbol completo de `.widget` (modal incluido) se compara contra el resto de la página como si fuera un solo elemento a `z-index: 10`, muy por debajo del carrusel (hasta 100). Subir el número del modal no podía arreglar esto nunca, sin importar qué tan alto se pusiera. | Se usó `createPortal(modalJsx, document.body)` (React) en ambos componentes — el modal se monta directo en `document.body`, fuera del subárbol de `.widget`, escapando su contexto de apilamiento por completo. Se auditaron el resto de modales de la app (`PlanetInfoModal`, `FeedbackButton`, `DonationButton`, `ProfileOnboarding`, `BannedGate`, `MusicPicker`) para confirmar cuáles vivían dentro de un ancestro con `position` + `z-index` propios — ninguno más estaba afectado (todos son hijos directos de `<main>`/`layout.tsx`, sin ese ancestro problemático), así que no se les aplicó el Portal innecesariamente. **Lección para el futuro**: cuando un modal sigue renderizándose detrás de otro elemento pese a tener un `z-index` ya alto, el diagnóstico correcto es revisar la cadena de ancestros DOM en busca de `position:fixed/absolute/relative` + `z-index` propio (que crea un nuevo contexto de apilamiento) — subir el número del modal es un síntoma de "no se revisó la causa raíz", no una solución, si el modal está anidado dentro de ese tipo de ancestro. |
| 2026-07-23 | Neptuno / sección informativa (`components/landing/PlanetInfoModal.module.css`) | Detectado en pruebas visuales con Playwright (no reportado por el usuario): al abrir el modal de información de un planeta, el planeta al FRENTE del carrusel (ej. Neptuno) se renderizaba ENCIMA del modal, tapando parte del texto (captura de pantalla real mostró "Tormenta de..." cortado por el sprite del planeta). | Las tarjetas del carrusel (`PlanetSelector.tsx`) usan `zIndex: Math.round(depth * 100)` para apilar la más al frente encima de las de atrás — la del frente llega hasta `z-index: 100`. `PlanetInfoModal.module.css` tenía `z-index: 60` en su overlay, muy por debajo de eso. | Subido a `z-index: 200` en el overlay del modal — cómodamente por encima de cualquier valor que el carrusel pueda generar. Verificado con captura de pantalla real tras el fix: el modal queda completamente por encima, texto legible de punta a punta. **Lección para el futuro**: cualquier overlay/modal nuevo debe considerar el `z-index` máximo real de los elementos ya existentes en esa misma pantalla (no asumir que un valor "razonable" como 50-60 es suficiente) — el carrusel en particular usa un rango dinámico calculado (0-100), no un valor fijo, fácil de no tener presente al escribir un componente nuevo aparte. |
| 2026-07-23 | Multijugador (`lib/multiplayer/useRoomConnection.ts`) | El usuario reportó: "cuando alguien es derrotado y sale el mensaje de la frase epica y mientras el otro miembro esta vivo el que fue derrotado le da salir de partida y eso provoca un reinicio en cero del que se quedo y pierde toda la puntuacion que llevaban" | El `useMemo` que construye `multiplayerConfig` tenía `players` en su arreglo de dependencias — el comentario original ya decía la intención correcta ("`allPlayersAdult`... no se re-evalúa si alguien se desconecta a mitad de partida"), pero la implementación no la cumplía: CUALQUIER cambio de roster (alguien sale, como en el reporte, o se une) recreaba el objeto `multiplayerConfig` COMPLETO con una referencia nueva. `GameCanvas.tsx` tiene `multiplayerConfig` en el arreglo de dependencias de su `useEffect` de montaje — una referencia nueva ahí desmonta y vuelve a montar `GameEngine` desde cero, y `mount()` llama a `useGameStore.getState().reset()` incondicionalmente, borrando nivel/vidas/estrellas del jugador que seguía jugando, aunque nada de la conexión real necesitara reiniciarse. | Se reescribió el `useMemo` para que dependa ÚNICAMENTE de `gameStart` (que el servidor fija una sola vez por partida y nunca vuelve a cambiar, ver `party/gameRoom.ts#startGame()`) en vez de `players` — `initialRoster`/`allPlayersAdult` siguen leyendo `players` dentro del cuerpo del memo, pero como ya no es una dependencia declarada, solo se calculan la primera vez (al arrancar) y el objeto entero mantiene su referencia estable el resto de la partida sin importar cuántas veces cambie el roster después. Dos intentos previos de arreglo (un `ref` mutado dentro del propio `useMemo`, y un `useEffect` separado llamando `setState` de forma síncrona) fueron rechazados por las reglas estrictas de ESLint de este proyecto (`react-hooks/refs`, `react-hooks/set-state-in-effect`) antes de llegar a esta versión final, que no necesita ningún `ref`/efecto adicional. Verificado con `tsc`/`eslint`/`npm run build` limpios. |
| 2026-07-23 | Habilidad de Saturno / progreso en equipo (`engine/characterSvg.ts`) | El usuario reportó: "cuando juegas el saturno no es el mismo que elegimos tiene los errores visuales del pasado" — el rediseño del anillo (dos mitades + degradado plateado, hecho el día anterior) no se veía en la partida real, aunque sí se veía correcto en el carrusel de selección de la landing. | El proyecto tiene **dos representaciones SVG independientes de cada personaje**: el componente React (`components/characters/Saturn.tsx`, usado solo por el carrusel de la landing) y un SVG en texto plano (`engine/characterSvg.ts`, rasterizado una vez a textura de PixiJS para la partida real vía `loadPlanetTexture()`). El cambio del anillo (ver fila del día anterior) solo se aplicó al primero — el segundo, que es el que realmente se ve jugando, se quedó con el diseño viejo (una sola elipse, siempre detrás del cuerpo, color dorado plano). Ninguno de los dos archivos importa del otro, así que TypeScript no pudo avisar de la inconsistencia. | Se replicó el mismo enfoque (elipse trasera + elipse delantera recortada con `clipPath` a su mitad inferior + `linearGradient` plateado) directamente en el SVG de texto de `engine/characterSvg.ts`, con IDs de gradiente/clip distintos (`rg`/`rc`) para no chocar con los ya existentes (`g`/`c`). **Lección para el futuro**: cualquier cambio visual a un personaje debe tocar AMBOS archivos en el mismo paso — el componente React (selección/menús) y el SVG de `characterSvg.ts` (textura real del juego) — o verificar explícitamente si el cambio aplica a los dos antes de darlo por terminado. |
| 2026-07-22 | Top 10 / leaderboard (`lib/schemas/leaderboard.ts`, `app/api/sessions/complete/route.ts`) | El usuario reportó: "en el top 10 solo aparece la imagen del planeta que mayor nivel alcanzó, falta en los demás registros" | `leaderboard.planet` se agregó (ver AGENTS.md §2.4) DESPUÉS de que ya existieran entradas reales de leaderboard — y `POST /api/sessions/complete` solo escribe/actualiza `planet` (junto con `bestScore`/`levelReached`/`achievedAt`) cuando el jugador SUPERA su propio mejor puntaje anterior. Cualquier entrada cuyo dueño no haya vuelto a superar su récord desde que se agregó el campo se quedó sin él para siempre — el campo estaba documentado como "opcional por compatibilidad", pero nunca se corrió un backfill real de los datos ya existentes (mismo tipo de brecha que el incidente de `termsAndConditions`/`game_config`, esta vez sobre una colección con muchos documentos en vez de un documento único). | Se corrió un script de un solo uso que reconstruyó `planet` para las entradas faltantes: `game_sessions.planet` NUNCA fue opcional (se guarda siempre, en cada partida), y la misma variable `now` de esa request se usa tanto para `game_sessions.endedAt` como para `leaderboard.achievedAt` — mismo timestamp exacto en ambos documentos. Se usó esa coincidencia (`playerId` + `endedAt` == `achievedAt`) para encontrar la sesión real que generó cada récord y copiar su `planet`, con un respaldo por `levelReached` más reciente para los pocos casos sin coincidencia exacta. Verificado directo en Mongo: las 5 entradas reales del leaderboard (algunas de prueba) quedaron con su planeta correcto tras correr el script. **Lección reforzada** (tercera vez en esta sesión, ver filas de `game_config`/`AdminConfigForm.tsx` más abajo): agregar un campo nuevo "opcional por compatibilidad" a un esquema sin correr un backfill real dejará datos incompletos en silencio hasta que alguien lo note por accidente — el patrón correcto es sembrar/backfillear el dato existente en el mismo momento en que se agrega el campo, no confiar en que se irá completando solo con el uso normal. |
| 2026-07-22 | Configuración granular de balance (`components/admin/AdminConfigForm.tsx`) | El usuario reportó un crash real en `/admin`: `TypeError: Cannot read properties of undefined (reading 'toLocaleString')` en `FormattedNumberInput.tsx`, con el stack apuntando a `AdminConfigForm` | Al cambiar el esquema `game_config.player.baseSpeed` (número único) a `game_config.player.baseSpeedByPlanet` (objeto con una clave por planeta, pedido explícito del usuario: "permite que las velocidades sean individuales por planeta"), se actualizó `lib/schemas/gameConfig.ts` y se corrió la migración contra Mongo, pero `AdminConfigForm.tsx` se quedó momentáneamente leyendo `config.player.baseSpeed` (ya no existe) en vez de `config.player.baseSpeedByPlanet[planet]` — el dev server seguía sirviendo esa versión intermedia mientras se terminaba el resto del cambio, y `FormattedNumberInput` recibía `value={undefined}` y tronaba al llamar `.toLocaleString()` sobre él. | Se corrigió `AdminConfigForm.tsx` de inmediato (antes de continuar con cualquier otro cambio) para iterar los 6 planetas y leer `config.player.baseSpeedByPlanet[planet]`, verificado con `tsc --noEmit` limpio. **Lección reforzada** (ya documentada en una fila anterior para `termsAndConditions`/`game_config` completo): cuando un cambio de esquema toca un campo que un componente de UI ya está leyendo activamente, el componente debe corregirse en el MISMO paso que el esquema, no como una tarea separada — de lo contrario, cualquier render intermedio del dev server expone un estado roto real al usuario, no solo un error de tipos que `tsc` habría atrapado si se hubiera corrido antes de seguir editando. |
| 2026-07-22 | Peligros avanzados por nivel (Sol Rojo/Agujero Negro Nova/frecuencia de asteroides triplicada) — riesgo identificado, no un bug reproducido | Al implementar el Sol Rojo (nivel 45+), el Agujero Negro Nova (nivel 60+) y su efecto de triplicar la frecuencia de asteroides mientras está activo, los tres quedaron gateados por `this.level` (nivel local del jugador) antes de llamar a `.update()`/de spawnear asteroides — exactamente la misma condición que causó el bug real de desincronización de multijugador documentado más abajo (`Sun.ts`, ronda #3): `level` es una variable LOCAL de cada cliente (cada jugador sube de nivel derrotando SU PROPIO agujero negro a clics, nunca sincronizado), y estos tres sistemas consumen del `rng` COMPARTIDO. | Se optó por NO resolverlo con una re-arquitectura completa (fuera de alcance de este pedido) y en cambio seguir el mismo patrón YA aceptado en el resto del motor: la frecuencia de aparición de asteroides (`asteroidTimer`) y de llamaradas (`nextFlareIn` en `Sun.ts`) YA dependían de `level` desde antes de esta ronda (solo el CONTEO de llamaradas por ráfaga fue el que se corrigió en la ronda #3, no la frecuencia con la que se generan) — es decir, el proyecto ya tenía este mismo tipo de deriva tolerada en producción, solo no catastrófica como el bug de conteo que sí se corrigió. Los objetos nuevos (`redSun`/`novaBlackHole`/`zodiacLayer`) se **construyen siempre** en `mount()`, sin condicionar por nivel (para no alterar el orden de consumo del rng al menos en ese punto) — solo su `.update()` se salta hasta el nivel correspondiente. **Queda documentado como limitación conocida**: en una sala multijugador donde los jugadores llegan a niveles muy distintos (45+/60+), el mundo compartido (posiciones exactas de asteroides/Sol/Agujero Negro) puede divergir entre clientes a partir de ese punto — mismo criterio que las "cotas de sanidad, no anti-cheat real" ya documentadas en AGENTS.md §7.2: aceptable para el tamaño y uso real de este juego familiar (los niveles 45+/60+ son de sesiones muy largas, más relevantes en solitario que en una partida grupal típica), no bloqueante para esta funcionalidad. La capa decorativa de constelaciones del zodiaco (`ZodiacLayer`) sí se aisló con su propio `rng` local independiente (no el compartido) precisamente para no sumar más riesgo de desincronización en algo puramente visual sin necesidad. |
| 2026-07-22 | Ronda de retroalimentación en vivo #5 (`lib/schemas/gameConfig.ts`, `lib/gameConfig.ts`) | El usuario reportó que el enlace de WhatsApp "se pierde en cada reinicio" — un dato que debía ser persistente en Mongo (ver AGENTS.md §7) parecía resetearse a vacío. | Se agregó `termsAndConditions: z.string()` como campo **obligatorio** de `GameConfigSchema` (ver AGENTS.md §6.5) y se corrió el nuevo texto de lanzamiento en `DEFAULT_GAME_CONFIG`, pero **la migración aditiva correspondiente (`scripts/seeds/seed-config.ts`) no se corrió de inmediato contra la base de datos real** tras el cambio de esquema. Durante esa ventana, el documento `game_config` en Mongo seguía sin ese campo — `GameConfigSchema.safeParse(doc)` en `getGameConfig()` fallaba la validación completa (no solo del campo faltante) y el `catch` implícito del `safeParse` fallido caía al `DEFAULT_GAME_CONFIG` **entero**, cuyo `whatsappLink` es `""` — el dato real seguía intacto en Mongo todo este tiempo, solo dejaba de leerse correctamente. Ningún dato se perdió de verdad. | Se corrió `npm run seed:config` (la migración aditiva ya existente, que nunca pisa valores ya personalizados por el admin — ver su propio comentario en el código) y se verificó con una consulta directa a Mongo que tanto `whatsappLink` (`https://wa.me/message/VHN4K4UW4WMAH1`) como `termsAndConditions` (2869 caracteres) quedaron presentes y correctos. **Lección para el futuro**: cualquier campo nuevo agregado como obligatorio a `GameConfigSchema` (o cualquier esquema leído con `safeParse` + fallback a un default) debe ir acompañado de correr su migración aditiva en el mismo momento, no como un paso posterior — un esquema más estricto que la base de datos real produce un fallback silencioso a TODOS los valores por defecto, no solo al campo nuevo, que se puede confundir fácilmente con una pérdida de datos real. |
| 2026-07-22 | Ronda de retroalimentación en vivo #3 (multijugador, `engine/entities/Sun.ts`) | El usuario preguntó "¿el multijugador no es igualito en tiempo real?" tras jugar con su hija: a él le apareció un Agujero Negro que ella nunca vio, y viceversa — la arquitectura de multijugador (ver AGENTS.md §8) es lockstep determinista por semilla compartida, así que en teoría ambos clientes deberían generar exactamente el mismo mundo | Investigación de código confirmó una desincronización real e irreversible del `rng` compartido: `Sun.ts` generaba la ráfaga de llamaradas con `Array.from({ length: count }, () => { this.rng()... })`, donde `count` depende de `level` — y `level` es una variable LOCAL de cada cliente (cada jugador sube de nivel derrotando SU PROPIO Agujero Negro a clics; nunca se comunica a los demás jugadores). En cuanto dos jugadores llegaban a niveles distintos, uno consumía más o menos números aleatorios del `rng` COMPARTIDO que el otro en esa misma llamada — desde ese instante exacto, absolutamente todo lo demás que depende del mismo generador (próximos asteroides, próxima aparición del Sol, próximo Agujero Negro) quedaba desincronizado PARA SIEMPRE entre ambos clientes, sin forma de recuperarse solo. Se revisó el resto del motor (`spawnSystem.ts`, `BlackHole.ts`) buscando el mismo patrón — solo apareció en `Sun.ts`. | Se cambió a consumir SIEMPRE `sunConfig.maxFlares` números aleatorios (constante que viene de `game_config`, idéntica para todos los clientes de la sala — a diferencia de `level`), usando solo los primeros `count` vía `.slice(0, count)`. Así el cursor del `rng` compartido avanza exactamente igual en todos los clientes sin importar en qué nivel esté cada quien localmente. Verificado: `npm run typecheck`/`lint` limpios y una partida solo de varios segundos sin errores de consola; la prueba con 2 navegadores reales en la misma sala quedó pendiente de una verificación visual futura (el entorno de `partykit dev` fue intermitente durante esta sesión, ver filas de abajo). |
| 2026-07-22 | Ronda de retroalimentación en vivo #2 (carrusel elíptico, `PlanetSelector.tsx`) | Después de implementar el carrusel, el usuario reportó que dar clic en un planeta "pierde la funcionalidad de poder elegirlo... se queda en el arrastre del carrusel" | `handlePointerDown` llamaba `e.currentTarget.setPointerCapture(e.pointerId)` en el contenedor **inmediatamente**, en cualquier pointerdown — incluso los que ocurrían sobre un botón hijo (un clic normal). Una vez capturado el puntero por el contenedor, el `click` del botón nunca llegaba a dispararse. Detectado con Playwright: `document.elementFromPoint` en el punto exacto del clic seguía mostrando el propio contenedor del carrusel, no el botón. | Captura perezosa: la captura del puntero solo se activa una vez que el movimiento supera `DRAG_MOVE_THRESHOLD` (6px), no en el pointerdown. Un clic simple (sin movimiento significativo) nunca la activa, y el botón recibe su evento `click` normalmente. Verificado real: clic aislado (sin arrastre previo) selecciona correctamente. |
| 2026-07-22 | Ronda de retroalimentación en vivo #2 (carrusel elíptico) | Tras rediseñar el giro por hover a "mover el mouse ya gira, directo y rápido" (pedido explícito del usuario), el simple gesto de acercar el cursor a un planeta para darle clic ya lo desplazaba lo suficiente para que el clic cayera fuera de él — verificado con Playwright: el elemento bajo el punto de clic pasó de ser el botón esperado a ser otro sibling o el contenedor. | Un solo evento `pointermove` con un `movementX` grande (el cursor "entrando" de golpe al área, o el salto sintético de Playwright) rota el carrusel varios grados de una vez; cerca del frente del óvalo, la derivada de `sin(ángulo)` es máxima, así que incluso pocos grados mueven el elemento decenas de píxeles — justo donde el usuario acababa de apuntar con el cursor. Un tope por evento (`MAX_HOVER_DELTA_PER_EVENT`) por sí solo no bastó: seguía desplazando ~34px, suficiente para fallar el clic. | Se agregó una "zona muerta" de entrada (`HOVER_DEADZONE_PX`, 16px): el giro por hover solo se activa una vez que el movimiento **neto** desde que el cursor entró/se detuvo supera ese umbral — un barrido real lo supera de inmediato, un simple acercarse a dar clic no. Verificado con un clic humano-realista (mover a un punto, esperar, dar clic sin más movimiento): selecciona correctamente. |
| 2026-07-22 | Ronda de retroalimentación en vivo #2 (carrusel elíptico) | El usuario reportó que tras un barrido rápido pero corto con el mouse, si el cursor simplemente se detenía (sin salir del área del carrusel), el giro se frenaba de golpe en vez de seguir girando y desacelerar gradualmente ("como una ruleta") | El "coast" (impulso + fricción) solo se disparaba en `pointerup`/`pointerleave` — si el cursor simplemente dejaba de moverse sin soltar ni salir, no había ningún evento que lo disparara, así que el giro se quedaba exactamente donde lo dejó el último `pointermove`. | Se agregó un temporizador de inactividad (`IDLE_COAST_DELAY_MS`, 70ms): si no llega otro `pointermove` en ese lapso, se asume que el cursor se detuvo y arranca el "coast" con el último impulso registrado; cualquier movimiento nuevo lo cancela y retoma control directo. Verificado con Playwright: tras un barrido corto y detener el mouse sin salir, el planeta objetivo siguió desplazándose visiblemente durante varios cientos de ms antes de asentarse. |
| 2026-07-22 | Ronda de retroalimentación en vivo #2 (diagnóstico, no un bug cerrado todavía) | Reporte real recurrente: el motor de juego "se reinicia" a nivel 0 a mitad de partida, sin ningún mensaje de derrota — le pasó al usuario en nivel 14 y 16+, y también en el celular de su hija jugando en multijugador (su jugador remoto, ya muerto, reapareció al centro de la pantalla) | Sin poder ver la consola de un dispositivo ajeno (el celular de la hija), la causa raíz no se pudo confirmar con certeza. Hipótesis más fuerte (no confirmada 100%): el propio asistente editaba en vivo archivos del motor (`GameEngine.ts`, `Sun.ts`, `InputController.ts`, `GameCanvas.tsx`) en el mismo servidor de desarrollo mientras el usuario/su hija jugaban — cada guardado dispara *Fast Refresh* de Next.js, y como esos archivos son dependencias no-componente de `GameCanvas.tsx` (un Client Component), React Fast Refresh remonta el componente completo, recreando un `GameEngine` nuevo que llama a `reset()` (nivel/vidas a su valor inicial) sin que exista ningún "game over" real. Un error real (`HOVER_TRANSITION is not defined`) que el usuario vio en el celular justo durante una edición en curso de ese archivo respalda la hipótesis. Dos bugs reales de gameplay SÍ se encontraron y corrigieron durante la misma investigación (ver filas de más abajo: crash de `Sun.setMouth`, jalón del Agujero Negro por el mouse) pero ninguno de los dos explica un reinicio SIN mensaje de derrota — son bugs distintos, reales, pero no necesariamente la causa de este reporte específico. | Se agregó trazabilidad real hacia el servidor (`app/api/client-log/route.ts`, `lib/clientLog.ts`, `components/shared/ClientErrorReporter.tsx` — captura errores/promesas no atrapadas de toda la app) más instrumentación específica del ciclo de vida del motor (`GameCanvas.tsx` registra cada montaje/desmontaje de su efecto con sus dependencias; `GameEngine.ts` registra cada `reset()` y cada `destroy()` con el stack completo de quién lo llamó) — todo visible en `logs/app.log`. **Sigue abierto**: no ha vuelto a ocurrir desde que se agregó el logging, así que la hipótesis de Fast Refresh no está confirmada con evidencia directa todavía — la próxima vez que pase, el log debe mostrar si hubo más de un `game.engine.reset` real en la sesión y desde dónde se llamó `destroy()`. |
| 2026-07-22 | Ronda de retroalimentación en vivo #2 (crashes reales durante el juego) | El motor tronaba en vivo con `Cannot read properties of null (reading 'clear')` en `Sun.setMouth`, a veces reiniciando la sesión de React | `Sun` programaba un `window.setTimeout` suelto de 380ms (para cerrar la boca tras hablar) sin rastrearlo ni cancelarlo. Si el motor se destruía (fin de partida, navegación, hot-reload de dev) mientras ese timer seguía pendiente, el callback llegaba después con `this.mouth` ya destruido por PixiJS (`app.destroy(true, {children:true})`) y `.clear()` sobre un Graphics destruido tronaba. Mismo patrón de bug que la fuga de memoria de PixiJS ya documentada arriba (recurso sin limpiar al destruir), pero esta vez con un timer en vez de una textura. | `Sun` ahora guarda el handle del timer (`mouthTimer`) y lo cancela en un `destroy()` nuevo, llamado desde `GameEngine.destroy()`; además `setMouth()` gana una guarda defensiva (`if (this.mouth.destroyed) return`) como segunda capa. Verificado con Playwright: se esperó a que el Sol hablara (fase "resting" con boca abierta) y se navegó fuera de inmediato — cero errores de consola tras el fix (antes sí se hubiera reproducido). |
| 2026-07-22 | Ronda de retroalimentación en vivo #2 (crashes reales durante el juego) | Usuario reportó llegar al nivel 14 y, al dar clic rápido varias veces (para derrotar al Agujero Negro), el juego "se reiniciaba" de golpe a nivel 0 — muy molesto por perder el progreso | Investigado y descartado un crash técnico (no hay guardas rotas ni loops sin control en `attackBlackHole`/`registerClick`/`reportSessionComplete`, que sí está correctamente protegido contra llamadas repetidas). La causa real es una interacción entre dos mecánicas correctas por separado: (1) el control de mouse "sigue al cursor" (ver fila de más abajo) mueve al jugador hacia dondequiera que esté el cursor, sin necesitar clic; (2) atacar al Agujero Negro es un input global (Spacebar/clic en cualquier lugar) que NO necesita apuntar, pero el instinto natural del jugador es dar clic ENCIMA del Agujero Negro para "apuntarle". Combinado, eso literalmente caminaba al jugador hacia el Agujero Negro mientras lo atacaba, sumándose a su propia atracción (que además crece hasta 3x mientras más tarda en derrotarse) — a nivel 14 (que exige hasta 20 clics, tomando varios segundos) había tiempo de sobra para recibir varios golpes de 2 vidas y perder las 10 de golpe, un game over real pero que se sintió como un reinicio aleatorio. | Se agregó una "zona de peligro" opcional a `InputController.getMovement()`: cuando el Agujero Negro está activo, el punto-objetivo del cursor se recorta al borde de su radio de daño antes de calcular hacia dónde moverse — dar clic encima de él para atacarlo ya no arrastra al jugador adentro, sin tocar la mecánica de atracción propia del Agujero Negro (que sigue premiando derrotarlo rápido) ni el resto del control. Verificado: `npm run typecheck`/`lint` limpios y una prueba de movimiento activo de mouse sin errores de consola; el escenario exacto (nivel 14, agujero negro maduro) no se pudo reproducir 1:1 en la ventana de prueba disponible, pero la causa raíz identificada por revisión de código es consistente con el síntoma reportado. |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | Copiar/pegar dejó una etiqueta JSX inválida `<stopColor="#f5d9a8" />` en `components/characters/Jupiter.tsx` | Error de redacción manual al escribir el SVG de Júpiter a partir del de Saturno — `stopColor` es un atributo de `<stop>`, no un elemento propio. | Detectado y corregido antes de que causara un error real de build (revisión proactiva del archivo recién creado, no un fallo reportado por el usuario). Lección: revisar visualmente cualquier SVG grande escrito a mano antes de darlo por bueno, no solo confiar en que TypeScript/ESLint lo detecten (JSX inválido de este tipo a veces sí pasa el parser). |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | La API de Mailpit devolvía texto plano `"Unauthorized."` en vez de JSON, rompiendo `JSON.parse()` en `pollMailpitForMagicLink()` (`DEV_AUTO_LOGIN`) | Faltaba el header `Authorization: Basic <base64(usuario:contraseña)>` en la llamada `fetch()` a la API HTTP de Mailpit — no se puede consultar mensajes sin autenticarse, igual que la UI web. | Se agregó el header Basic Auth construido desde `EMAIL_SERVER_USER`/`EMAIL_SERVER_PASSWORD` (las credenciales ya confirmadas en AGENTS.md §0). Verificado: el flujo completo de auto-login (pedir enlace → sondear Mailpit → seguir el enlace → sesión creada) funcionó de punta a punta después del fix. |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | Cerrar sesión desde el celular dejaba la sesión "atrapada": la UI seguía mostrando el botón "Salir" y no reaccionaba a ningún clic, incluso después de borrar caché | El Router Cache de cliente de Next.js seguía sirviendo una versión ya renderizada de la página después de que `signOutAction()` (Server Action) llamaba a `redirect()` internamente — confirmado con Playwright: `GET /api/auth/session` devolvía `null` del lado del servidor inmediatamente después del logout, pero la UI seguía mostrando el estado de sesión activa hasta un recargo manual completo. `revalidatePath()` dentro de la Server Action tampoco lo resolvió. | `signOutAction()` ya no llama a `redirect()` — solo hace `await signOut({redirect:false})` y devuelve el control al cliente; `AuthStatus.tsx` hace `window.location.href = "/"` (navegación dura del navegador) después de que la acción resuelve, evitando el Router Cache por completo. Mismo patrón aplicado al login (`handleRequestMagicLink`) por el mismo motivo. |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | En PC/laptop, mover el mouse **sin hacer clic** no movía al personaje — el usuario lo reportó dos veces, la primera corrección fue insuficiente | Primer intento: se hizo que el mouse funcionara con arrastre (clic + mover), igual que el touch — pero el uso real de un touchpad de laptop no incluye mantener presionado, el usuario esperaba que el personaje **siguiera el cursor** con solo mover el mouse. | Rediseño completo de `engine/controls/InputController.ts`: separación real entre control de mouse (seguimiento de cursor vía `pointermove` pasivo, sin `pointerdown`, con zona muerta de 20px y radio de seguimiento de 160px para la rampa de velocidad) y control táctil (se mantiene el joystick de arrastre original, que sí es el patrón correcto para touch). Verificado con Playwright: se movió el mouse sin hacer ningún clic y se confirmó con capturas antes/después que el sprite del jugador se desplazó hacia el cursor. |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | Auth.js redirigía al callback de Magic Link a `http://localhost:3000` en vez de la IP LAN real (`192.168.101.7:3000`) cuando se abría el juego desde un celular en la misma red | `console.log` de depuración en el callback `redirect` de Auth.js mostró que su `baseUrl` interno se calculaba distinto entre la petición POST que genera el correo (correcto) y la petición GET del callback del enlace (incorrecto, resolvía a `localhost`) — un comportamiento específico del dev server/Turbopack de Next.js 16, no un ajuste de configuración documentado como intencional. Los headers `x-forwarded-host`/`host` sí traían la IP correcta en ambos casos. | Se agregó un `TRUSTED_DEV_REDIRECT_ORIGINS` (allowlist explícita de IPs LAN de desarrollo) y un callback `redirect` propio en `lib/auth.ts` que confía en esos orígenes en vez del `baseUrl` interno de Auth.js. El mismo patrón de bug (`new URL(request.url).origin` resolviendo a `localhost` pese a que los headers traían la IP real) se encontró de forma independiente en `app/api/donations/checkout/route.ts` — mismo síntoma, mismo entorno, dos ubicaciones distintas del código; se resolvió de forma genérica con `lib/requestOrigin.ts#getRequestOrigin(request)`, que deriva el origen de los headers en vez de `request.url`. |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | El juego se volvía notablemente lento tras llegar a nivel ~9, al punto de que el usuario tuvo que cerrar la pestaña del navegador para recuperar el laptop | Fuga de memoria real de PixiJS: `gameLayer.removeChild(sprite)` quita el sprite del árbol de render pero **no** libera sus recursos internos (buffers de batching/GPU) — hace falta llamar `sprite.destroy()` explícitamente. El motor removía asteroides/pulsares/estrellas/jugadores continuamente sin nunca destruirlos, acumulando memoria partida tras partida y nivel tras nivel. | Se agregó `.destroy()` en los 5 puntos de remoción de entidades de `GameEngine.ts` (consumo por el Agujero Negro, salida de pantalla, destrucción de la luna de la Tierra, destrucción de la lava de Marte, colisión en el loop principal) y en la limpieza de jugadores remotos, con cuidado de pasar `{texture:false, textureSource:false}` para no destruir las texturas cacheadas y compartidas por el resto de entidades del mismo tipo (lo cual habría roto a todos los demás asteroides que comparten esa textura). |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | El HUD mostraba 0 estrellas al empezar una partida nueva, aunque el jugador ya llevaba 200+ acumuladas de sesiones anteriores | El store de Zustand del motor (`gameStore.ts`) solo llevaba `starsThisRun` (estrellas ganadas en la partida actual), nunca recibía el total acumulado real del jugador (`session.stars`) al iniciar. | Se agregó un 4º parámetro `initialStars` al constructor de `GameEngine` (leído de `useGlobalContext()` en `GameCanvas.tsx`) y un campo `initialStars` en `gameStore` que se preserva entre `reset()` (a diferencia de `starsThisRun`, que sí se reinicia); el HUD ahora muestra `initialStars + starsThisRun`. |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8) | En móvil, el botón de activar habilidad y el botón de atacar al Agujero Negro terminaron siendo el mismo botón por error | Al agregar el botón flotante de ataque al Agujero Negro se reutilizó por descuido el mismo elemento/posición del botón de habilidad en vez de crear uno independiente. | Se separaron en dos botones independientes en `GameHud.tsx`/`.module.css`: habilidad (inferior-derecho, siempre visible en táctil) y ataque al Agujero Negro (inferior-izquierdo, morado con animación de pulso, solo visible cuando `blackHolePhase === "active"`). |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8, operación, no código) — recurrencia | Dos caídas internas del dev server de Next.js/Turbopack ("Jest worker encountered 2 child process exceptions, exceeding retry limit" + `EPIPE` repetidos), reportadas por el usuario como errores en pantalla | Se confirmó ambas veces que **no** eran causadas por el código del proyecto: se corrió `tsc --noEmit` excluyendo los artefactos de build de `.next/` y el resultado fue limpio en ambas ocasiones — es una inestabilidad interna del worker de Next.js/Turbopack en este entorno, no una regresión. | Se mató el proceso (`taskkill //PID <pid> //F`) y se reinició `npm run dev` limpio en ambos casos; el juego volvió a funcionar sin ningún cambio de código. Documentado explícitamente para no perseguir un "bug" inexistente si vuelve a ocurrir — revisar primero si es esto antes de asumir una regresión. |
| 2026-07-22 | Ronda de retroalimentación en vivo (post-Fase 8, operación, no código) — recurrencia confirmada | El lobby volvió a mostrar "Jugadores en la sala (0/4)" después de editar `party/*.ts` para agregar Júpiter/Saturno al multijugador, con el dev server de PartyKit corriendo | El usuario preguntó explícitamente si esto ya estaba documentado — **sí**: es el mismo problema de hot-reload de `partykit dev` ya registrado en la fila de abajo ("`npx partykit dev` arrancó en el puerto 1407..."), donde editar archivos de `party/*.ts` con el proceso corriendo puede hacer que deje de servir el party `directory` (404) mientras `main` sigue funcionando. **Nota importante**: esta NO es la misma causa raíz que la primera vez que apareció "0/4" en la sesión (esa fue `NEXT_PUBLIC_PARTYKIT_HOST=localhost:1999` inutilizable desde un celular, resuelta con `lib/multiplayer/partykitHost.ts`) — son dos bugs de "0/4" distintos que solo comparten síntoma. | Reinicio limpio del proceso de `partykit dev` (no depender del hot-reload tras editar archivos de `party/*.ts`), igual que la vez anterior. No requiere cambio de código — es una limitación conocida de la herramienta `partykit dev`, ya documentada en la fila correspondiente de este registro y en `QUICK-START.md`. |
| 2026-07-22 | 5 — Multijugador (bug encontrado durante pruebas de Fase 8) | El motor de juego se rompía con `this._cancelResize is not a function` justo después de la pantalla "Creando sesión del juego..." en una prueba real de 2 navegadores — el `<canvas>` nunca llegaba a montarse | `GameEngine.destroy()` llamaba incondicionalmente a `this.app.destroy()` de PixiJS. React Strict Mode (activo por defecto en `next dev`) invoca el efecto de montaje dos veces (montar → limpiar → montar); si el `cleanup` llega mientras `await this.app.init(...)` todavía no resolvía, se intentaba destruir una `Application` a medio inicializar, y PixiJS rompía al intentar quitar un listener de resize que nunca llegó a registrarse. Detectado con Playwright capturando `pageerror` de la consola del navegador (no se había hecho antes en las pruebas de Fase 5). | Se agregó una bandera `initialized` (ver `engine/GameEngine.ts`): `destroy()` solo llama a `app.destroy()` si `init()` ya resolvió; si `destroy()` llega antes, `mount()` detecta `this.destroyed` justo después de que `init()` resuelve y hace la destrucción real ahí mismo, sin construir nada más. Reproducido y verificado corregido con 2 navegadores reales (login → sala → "Iniciar ahora" → pantalla de transición → canvas montado sin errores en consola en ambos). |
| 2026-07-22 | 5 — Multijugador (bug encontrado durante pruebas de Fase 8) | `party/gameRoom.ts#onClose` difundía un `playerLeft` con el id de un jugador que **nunca llegó a unirse** | Cuando `onConnect` rechaza una conexión (sala llena / partida en curso) llama a `connection.close()` antes de agregarla a `this.players` — pero cerrar la conexión igual dispara `onClose`, que emitía `playerLeft` incondicionalmente para cualquier id, existiera o no en el roster. Detectado con un script de prueba con WebSockets reales (4 jugadores llenando la sala + un 5º rechazado) mientras se verificaba el nuevo directorio de salas abiertas. | `onClose` ahora solo difunde `playerLeft` y recalcula el roster si `this.players.delete(connection.id)` de verdad borró algo — si la conexión nunca estuvo en el roster, no hace nada. Verificado con el mismo script: el 5º cliente recibe `roomFull` y se cierra sin generar ningún evento fantasma para los 4 jugadores reales. |
| 2026-07-22 | 5 — Multijugador (operación, no código) | `npx partykit dev` arrancó en el puerto `1407` en vez del `1999` esperado (`NEXT_PUBLIC_PARTYKIT_HOST`), y tras reiniciarlo seguía sin liberar el puerto | Procesos `workerd.exe` (el runtime de PartyKit) de una sesión de pruebas anterior habían quedado huérfanos ocupando el puerto 1999 — Windows no los liberó al terminar el proceso padre de `npx partykit dev`. Un segundo síntoma relacionado: tras editar `party/gameRoom.ts` con el dev server corriendo, el *hot reload* de `partykit dev` a veces deja de servir el segundo party (`directory`) definido en `partykit.json` (`404 Party directory not found`) aunque el party `main` sí siga funcionando. | Se identificaron y terminaron los procesos `workerd.exe` huérfanos (`tasklist`/`taskkill` por PID) antes de reiniciar `partykit dev`. Para el segundo síntoma, un reinicio limpio del proceso (no depender del hot reload) vuelve a registrar ambos parties correctamente. Documentado como nota operativa en `QUICK-START.md` — no es un bug de código del proyecto. |
| 2026-07-22 | 3 — Motor de Juego | **No se pudo validar de forma confiable el criterio "60 FPS estable en escritorio"** | El navegador headless de Playwright en este entorno usa `SwiftShader` (renderizado WebGL por software, sin GPU real) — confirmado inspeccionando `WEBGL_debug_renderer_info`. La medición de ~11-12 FPS obtenida es un artefacto del entorno, no del código. | Se documenta honestamente como **no validado** en vez de reportar un falso "60 FPS confirmado" o alarmar con un falso "problema de rendimiento". El resto de la Fase 3 sí se probó exhaustivamente (0 errores de consola/runtime, todas las mecánicas documentadas funcionando visualmente). Pendiente: confirmar FPS real cuando el usuario corra `npm run dev` en su propio hardware con GPU. El código ya sigue las prácticas de rendimiento documentadas (texturas cacheadas una sola vez, pool de partículas con objetos reciclados, sin `Math.random()`/allocaciones pesadas por frame). |
| 2026-07-22 | 3 — Motor de Juego | El jugador se renderizaba como un **cuadrado negro sólido** en vez del planeta seleccionado | Las plantillas SVG usadas como textura (`engine/characterSvg.ts`) solo tenían `viewBox`, sin `width`/`height` explícitos. Un `<img>` cargando un SVG por data-URI sin dimensiones explícitas no siempre resuelve un tamaño intrínseco válido, y `Texture.from(img)` termina con una textura inválida. | Se agregó `width="200" height="200"` a las 4 plantillas SVG de planetas. Verificado con captura real: Mercurio se renderiza correctamente (cráteres, ojos, sonrisa) en el canvas de PixiJS. |
| 2026-07-22 | 3 — Motor de Juego | La capa de decoración del fondo (paralaje) se veía como un patrón de lunares repetitivo, no como nebulosas dispersas | El tile de la textura de decoración era muy pequeño (512px) con 4 manchas por tile a `alpha:0.18`, así que el patrón de repetición del `TilingSprite` era demasiado evidente. | Se subió el tile a 1400px, se bajó a 2 manchas por tile y `alpha:0.1` — mucho menos repetitivo. Queda como mejora pendiente para un pase de pulido visual futuro, no bloquea la Fase 3. |
| 2026-07-22 | 7 — Pagos | El tope de la barra de donación estaba mal: puse $1,000 MXN de máximo cuando la especificación real es $10,000 MXN | Definí `SLIDER_MAX_CENTS` a partir de una suposición propia en vez de confirmar el rango exacto con el usuario antes de implementar. | Corregido a `1000000` centavos ($10,000 MXN) tras la aclaración del usuario. El mecanismo en sí (atributos nativos `min`/`max`/`step` del `<input type="range">`, que impiden bajar del mínimo) ya se había probado funcionando en el pago real de extremo a extremo — el fix es un valor constante, no lógica nueva, así que no se repitió la prueba completa de pago; se validó con build/typecheck/lint limpios. |
| 2026-07-22 | 7 — Pagos | Las claves de Stripe provistas inicialmente (prefijo `51JhYXEAhrCwZBBUT...`) no funcionaban | Se llamó directo a `GET https://api.stripe.com/v1/account` con la `STRIPE_SECRET_KEY` guardada y Stripe respondió `"Invalid API Key provided"` — la llave no era válida en absoluto, no era un problema de cuenta/CLI ni de configuración del proyecto. | Se pidió al usuario una llave revisada. Las nuevas (prefijo `51TqTY91BIss5hYEo...`) sí verificaron correctamente contra la API y además coincidían exactamente con la cuenta ya logueada en el Stripe CLI local (`acct_1TqTY91BIss5hYEo`, "Mi Negocito") — resolviendo de paso un posible desajuste de cuenta entre el CLI y las claves del servidor. Lección: verificar credenciales de terceros contra la API real antes de asumir que un fallo posterior (ej. en el checkout) es un bug del código propio. |
| 2026-07-22 | 7 — Pagos | Duda inicial sobre si `stripe listen` generaría un secreto de webhook distinto al que el usuario ya tenía guardado (`whsec_23ee...`) | Se asumió que `stripe listen` siempre genera un secreto efímero por sesión, distinto de cualquier endpoint configurado en el Dashboard — resultó ser una suposición incorrecta para esta cuenta/versión del CLI: `stripe listen --print-secret` devolvió exactamente el mismo `whsec_` que el usuario ya había compartido. | Se verificó empíricamente en vez de descartar el valor del usuario por la suposición: se corrió `stripe listen --print-secret`, se comparó contra el valor dado, coincidieron, y se usó tal cual. El pago de prueba real confirmó que era el secreto correcto (los 6 eventos del webhook respondieron `200`). |
| 2026-07-22 | 6 — Persistencia | `GET /api/leaderboard` devolvía `{entries: []}` aunque Mongo sí tenía una entrada | El route tenía `export const revalidate = 60`, lo que hizo que Next.js lo pre-renderizara como estático **en build time** (cuando el leaderboard estaba vacío) — servía esa respuesta obsoleta hasta que algo disparara una revalidación en segundo plano. Detectado comparando la respuesta de la API contra lo que sí mostraba la landing (que lee Mongo directo y no tenía el problema). | Se cambió a `export const dynamic = "force-dynamic"` — nada de la UI actual consume este endpoint todavía (la landing usa `lib/leaderboard.ts` directo), pero si algún cliente lo llama en el futuro debe ver datos reales, no una foto congelada del momento del build. |
| 2026-07-22 | 5 — Multijugador | **Decisión de arquitectura** — el diseño original (`AGENTS.md §8`) decía que PartyKit simularía la física de asteroides/Sol/agujero negro | Cloudflare Durable Objects (donde corre PartyKit) no tiene DOM/Canvas — habría exigido portar todo `engine/entities/` y `engine/systems/` a una versión sin PixiJS que corriera en el servidor, duplicando lógica y arriesgando desincronía con el motor visual ya validado en Fases 3-4. | Se optó por **simulación determinista (lockstep)**: el servidor solo reparte un `seed`; cada cliente genera el mismo mundo localmente con un RNG con semilla (`engine/rng.ts`). Se refactorizaron `Sun`, `BlackHole`, `FieldObject` y `spawnSystem` para recibir el `Rng` inyectado en vez de llamar `Math.random()`. Verificado con 2 navegadores reales: mismo asteroide y misma estrella en la posición exacta en ambas pantallas. PartyKit quedó mucho más simple (solo lobby + relevo de posiciones), sin sacrificar el requisito de mundo compartido. |
| 2026-07-22 | 5 — Multijugador | Riesgo conocido, no bloqueante: el auto-inicio del lobby (180s) usa `setTimeout` en `party/gameRoom.ts` | Cloudflare Durable Objects puede hibernar una sala inactiva; un `setTimeout` en memoria no sobrevive a la hibernación en producción (sí funciona en `partykit dev`, que mantiene el proceso vivo). | Documentado como pendiente de endurecer antes de producción: migrar a la API de alarmas de PartyKit (`onAlarm`), que sí persiste a través de hibernación. No se tocó ahora porque el entorno de pruebas (`partykit dev`) no expone el problema y añadir alarmas sin poder probarlas localmente habría sido código no verificado. |
| 2026-07-22 | 3 — Motor de Juego | `@pixi/particle-emitter` (documentado en la especificación original) resultó incompatible con el proyecto | Su última versión (5.0.10) fija `peerDependencies` de PixiJS a `<8.0.0`; el proyecto instala PixiJS 8.19.0 (versión estable vigente). | Se construye un sistema de partículas propio y ligero (`engine/particles/ParticlePool.ts`) en vez de la librería — cubre exactamente los 3 efectos necesarios (llamaradas, lava de Marte, disco de acreción) sin quedar atado a una versión vieja de PixiJS. Documentado en `AGENTS.md` §4. |
| 2026-07-22 | 2 — Landing | En móvil, el widget de sesión (`position: fixed; bottom`) tapaba y bloqueaba el clic sobre la tarjeta de Mercurio al hacer scroll | Un elemento `fixed` permanece sobre el contenido aunque se haga scroll — no se había probado el scroll completo en viewport móvil, solo el viewport inicial. Detectado con una captura de página completa (`playwright screenshot --full-page`) en 390×844, no solo el primer viewport. | En escritorio se mantiene `fixed` (no hay colisión de scroll ahí); en móvil (`max-width: 640px`) pasa a vivir en el flujo normal del documento, arriba de todo. Se agregó también `flex-wrap` al formulario porque el botón "Entrar con Magic Link" se cortaba en 390px de ancho. Verificado con captura después del fix: los 4 planetas quedan completamente visibles y clicables. Refuerza la regla de [AGENTS.md §3.1](./AGENTS.md#3-estandares-obligatorios-de-codificacion-y-diseno-de-componentes) de probar página completa, no solo el viewport inicial, en cualquier pantalla con contenido que hace scroll. |
| 2026-07-22 | 1 — Auth | Login por Magic Link fallaba con `HTTP 500` / error `UntrustedHost` en `npm run start` (producción local) | Auth.js v5 no confía en el header `Host` de la request por defecto fuera de Vercel (que lo autodetecta); `next start` local no cuenta como entorno confiable automáticamente. | Se agregó `trustHost: true` explícito en la config de `NextAuth()` (`lib/auth.ts`) — es la práctica recomendada por Auth.js para despliegues donde controlas la infraestructura. Verificado con el flujo completo (CSRF → POST signin → email en Mailpit → GET callback → sesión creada) contra `npm run start`. |
| 2026-07-22 | 1 — Auth | `npm install @auth/mongodb-adapter` falló con `ERESOLVE` (conflicto de peer dependency) | El adapter fija `peer mongodb@"^6"`, pero la Fase 0 había instalado `mongodb@^7.5.0` (la última mayor disponible, sin fijar versión explícita). | Se bajó el driver a `mongodb@6.21.0` (última de la serie 6.x) — API sin cambios relevantes para nuestro uso (`MongoClient`, `.connect()`, `.db()`, `ping`). Se evitó `--legacy-peer-deps`/`--force` para no enmascarar el conflicto real. |
| 2026-07-21 | 1 — Auth | `npm audit` reporta 3 vulnerabilidades altas/moderadas (`postcss@8.4.31` XSS, `sharp@0.34.5` CVEs de libvips, `nodemailer` SSRF vía opción `raw`) | Las tres son dependencias **transitivas fijadas por `next@16.2.11`** (postcss/sharp) y por `@auth/mongodb-adapter`→`@auth/core` (nodemailer) — no son elegidas por nosotros y no hay versión propia que subir. `npm audit fix --force` sugiere degradar a `next@9.3.3`, lo cual rompería toda la app — **no se ejecuta**. | Se documenta como riesgo monitoreado, no como bloqueante: (1) `postcss` XSS solo aplica si se reinyecta CSS no confiable a un `<style>`, cosa que no hacemos; (2) `sharp`/libvips solo se activa si `next/image` procesa imágenes subidas por usuarios, y el proyecto no acepta imágenes de usuario (assets son SVG/CSS, ver AGENTS.md §4); (3) nodemailer's opción `raw` vulnerable nunca se expone a input de usuario (el email de Magic Link lo genera Auth.js con plantilla propia). Se agrega a `DEPLOYMENT.md` como checklist re-ejecutar `npm audit` antes de producción, esperando que Next.js/Auth.js publiquen parches. |
| 2026-07-21 | 0 — Fundación | `docker compose up -d` falló: "port is already allocated" en 1025/8025/27019 | Los puertos documentados en `INFRA.md`/`docker-compose.yml` ya estaban ocupados por contenedores `magic-link-mongo`/`magic-link-mailpit` de **otro proyecto** en la misma máquina, corriendo con las mismas credenciales (`admin`/`magiclink123`) que el usuario había confirmado para Planet Scape. | Se eliminaron los contenedores `planet-scape-mongo`/`planet-scape-mailpit` recién creados (nunca llegaron a iniciar) y se verificó que el Mongo compartido ya existente acepta las credenciales documentadas (`db.adminCommand({ping:1})` → `{ok:1}`). Planet Scape **reutiliza el Mongo/Mailpit locales compartidos** de la máquina de desarrollo (aislado por `MONGODB_DB=planet_scape`, una base de datos separada dentro de la misma instancia); no se necesitó levantar contenedores propios para validar la Fase 0. Pendiente: confirmar con el usuario si quiere mantener este stack compartido o migrar Planet Scape a puertos propios en `docker-compose.yml` para aislar ambos proyectos completamente. |
